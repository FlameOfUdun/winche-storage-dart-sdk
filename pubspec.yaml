name: winche_storage
description: Dart SDK for the WincheStorage backend - a reference-based API with resumable, multipart-aware upload and download tasks for any storage backend.
version: 1.0.0
repository: https://github.com/FlameOfUdun/winche-storage-dart-sdk
issue_tracker: https://github.com/FlameOfUdun/winche-storage-dart-sdk/issues

environment:
  sdk: ^3.0.0

dependencies:
  dio: ^5.9.2
  mime: ^2.0.0

dev_dependencies:
  test: ^1.24.0
  lints: ^4.0.0
